# zcode 任务追加 — AP 画作外显接入（2026-07-03 第七批，接 T 批次）

**Fable5 本批已完成的底层（全部实测验证）**:
1. `/api/phase20_7/turn` 响应新增 **`inner_pictures`** 字段——turn 内想象召回/视觉重建产出的
   内心画面 PNG 的 URL 列表（`[{url, tick, action_type, source, clarity_coverage}]`，最多 6 张）。
   已含红线过滤（只暴露状态池 canvas 渲染的图，原图缩略图不会出现）。
2. **"画X"教学路径验证通**: 首问"画一个苹果"不会 → 教师反馈"是苹果"(reward) → 短语经指代
   共现绑定 → 再问触发想象召回 → PNG 产出。**没教过的"画Y"不触发**（可教指代，非关键词路由）。
3. **cells 字段确认在 turn JSON 里**（`tick_trace[i].draft_grid.cells`）——你之前报"没返回"
   是读取位置问题: 它在每个 tick event 的 draft_grid 里，不在顶层。竖式过程重放用它。

**必读**: ColdSave 第十一节（两层目标裁定 + 诚实文案边界）。

**红线**:
- "AP 画的画"文案必须写"**它把想象中的画面呈现给你**"，不许写"它会画画/它画了一幅画"
  （逐笔绘制是第二层目标，未做——见 ColdSave 十一节）。
- 不动 runtime/*.py 和 web_chat.py。

---

## S1. AP 气泡内嵌想象画面

turn 响应含非空 `inner_pictures` 时，AP 回复气泡下方显示这些图（小尺寸缩略，点击放大）,
配一行小字: "这是它想象中的画面（焦点处清晰，周边模糊——它真的在'看'自己的记忆）"。
- 验收: 教苹果图后发"苹果"，气泡下出现想象画面缩略图，点击可放大。

## S2. "教它画画"引导卡

新手引导加第 6 张卡（三步格式同其它卡）:
- 标题: 教它想象画面
- 看示例: 点"多模态识图示例"教它认一张图
- 自己教: "先发一张图教它这是什么 → 然后发'画一个X'（它不会）→ 反馈教它'是X' →
  再发'画一个X'，它会把想象中的画面呈现给你"
- 验收: 按卡片步骤手动走通一次。

## S3. 详情页"AP 的画板"区块

草稿格旁边加"AP 的想象画面"区块: 显示当前 session 最近的 inner_pictures（沿 turn 响应
累积在前端即可，不需要新接口），每张带 tick 号和触发行动名。区块说明文字:
"AP 的内心画面——从它的状态池视觉记忆渲染，焦点清晰周边模糊。逐笔作画（画板行动器）
在路线图 APV3.1。"
- 验收: 区块存在且随对话更新。

## S4. 竖式过程重放修正

用你之前没找到的正确路径: `turn.tick_trace[i].draft_grid.cells`（每 tick 的非空单元快照，
含 written_at_tick）。重放逻辑: 取最后一个 tick 的 cells，按 `tick` 字段升序逐格淡入。
数学 turn（如 42+35=?）的答案逐位写入顺序由此可见。
- 验收: 跑一次数学演示，点"查看草稿过程"能看到逐格重放。

---

执行顺序: S4（修正已有功能）→ S1 → S3 → S2。完成后连同 U/T 批次一起交 Fable5 终审。
