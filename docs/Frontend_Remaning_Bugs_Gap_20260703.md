# 前端剩余bug/缺口清单 (2026-07-03)

## codex已做的（验证通过）
- 4页面tab导航 ✓
- 画板卡片 (canvas画笔+上传) ✓
- progress等待态接口 ✓
- TTS试听绑定当前选中voice ✓
- 引导卡5张 ✓
- 主观能动性示例按钮 ✓

## 仍需修复的bug/缺口

### 纯前端可做（不需fable5）
1. **三列排版** — 右侧内心画面/想法云/内心音频从两列变三列，不再挤。HTML改布局即可。
2. **审计曲线优化** — 去无意义0噪声+加坐标轴+缩小尺寸+防止曲线比轴宽。前端JS canvas渲染改。
3. **想法云浮动效果** — 照搬旧版半透明碰撞浮动。CSS+JS改。
4. **记忆包筛选UI** — 有memoryPackageSelection数据结构但无UI预览/勾选/翻页/反选。前端加交互。
5. **理论页内容扩充** — 白皮书整理成2-5万字分章分页。纯HTML文本内容。
6. **详情页卡片样式** — tick回放/学习闭环等改成卡片分类美化。CSS改。
7. **引导卡三步格式** — 每张卡改为"标题+点示例看+自己教怎么做"三步。HTML改。

### 需fable5底层支持（前端做不了）
8. **草稿格竖式过程可视化** — tick trace里无`cells`字段（DraftGrid的二维单元列表）。
   fable5说做了但实际没在`_tick_event`里序列化。需fable5在runtime的tick event里加
   `grid.cells`的非空单元列表 `(row,col,char,tick)`。前端拿到后可逐格淡入重放竖式过程。

9. **AP画板/辅助线** — AP在用户发的图片上画辅助线（§66 画板闭环），当前phase20_7
   无canvas输出行动器（AP只能写DraftGrid文本不能画图输出）。这是架构级缺口，
   需fable5评估是否适合当前底座。

10. **记忆包按记忆/范式筛选** — 后端有list/export/import接口，但筛选参数
    （按时间/关键词召回/按session等）需要确认interface完整可用。

## 给fable5的底层任务清单
- T-D: runtime tick event加`grid.cells`序列化（草稿格逐格过程可视化前置）
- T-E: 评估AP画板输出/辅助线能力（§66画板闭环——AP画线后重新看见→成为视觉SA→再推理）
  如果适合当前底座就做，不适合就出计划
- T-F: 记忆包筛选接口确认（按时间/关键词/范式的筛选参数是否完整）